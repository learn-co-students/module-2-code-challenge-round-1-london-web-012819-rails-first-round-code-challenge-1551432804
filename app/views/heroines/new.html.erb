<h1>Create a New Super Heroine!</h1>
<% if @heroine.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@heroine.errors.count, "error") %> prohibited this heroine from being saved:</h2>

    <ul>
    <% @heroine.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_for @heroine do |f| %>
Name: <%= f.text_field :name %><br/><br/>
Super Name: <%= f.text_field :super_name %><br/><br/>
Assign a Power: <%= f.collection_select(:power_id, Power.all, :id, :name, prompt: true) %><br/><br/>

<%= f.submit %>

<% end %>
